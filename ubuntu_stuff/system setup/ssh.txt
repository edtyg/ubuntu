ssh = secure shell
/etc/ssh/		 		# standard location


sudo apt update
sudo apt upgrade

sudo apt install openssh-server
sudo apt-get install ssh
sudo ufw allow ssh


ip addr 				# can lookup your ip address here - ssh default port is 22

grep -i port /etc/ssh/sshd_config 	# can check port here

sudo nano /etc/ssh/sshd_config		# can update file here - uncomment by taking out # higher security by changing default port
sudo systemctl restart sshd 		# restart it

sudo ss -tulpn | grep ssh 		# check if listening on port correctly